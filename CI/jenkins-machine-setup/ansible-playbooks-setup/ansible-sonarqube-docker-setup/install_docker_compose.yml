---
- name: Install SonarQube
  hosts: your_sonarqube_server
  become: yes

  tasks:
    - name: Install Docker and Docker Compose
      apt:
        name: "{{ item }}"
        state: present
      loop:
        - docker.io
        - docker-compose
